services:
  api:
    image: example.invalid/seedhouse/api:1.4
    env_file:
      - .env.production
    environment:
      SESSION_KEY: ${SESSION_KEY}
    secrets:
      - tls_certificate
secrets:
  tls_certificate:
    file: ./fixtures/certificate.pem
